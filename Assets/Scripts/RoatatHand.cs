using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoatatHand : MonoBehaviour
{
    public Transform target; 
    
    // Update is called once per frame
    void Start()
    {
        // transform.Rotate (new Vector3 (90, 0, 0));
    }
}
